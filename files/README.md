Пакеты OpenBao 2.6.2 для x86_64 уже лежат здесь: deb, rpm и `checksums.txt`.

`tls.crt` и `tls.key` необязательны. Если их нет, `install.sh` выпустит сертификат на IP этой машины. Свой сертификат можно положить сюда до запуска, в репозиторий он не попадает.
